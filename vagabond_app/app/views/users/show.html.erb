<div>
  Welcome, <%= @user.name %>!
  <br>
  <%= @user.current_city %>
  <br>
  Joined <%= @user.created_at %>

<br>
<h2>Posts:</h2>
  <% @user.posts.each do |post| %>
  <h3> <%= link_to post.title, post_path(post) %> </h3>
  <% end %>

</div>

<div>
<h2><%= @city.name %></h2>
</div>

<div>
  <%= image_tag "sf.png" %>
</div>

<% @city.posts.sort_by{|post| [post['updated_at']]}.reverse.each do |post| %>
<h3> <%= link_to post.title, post_path(post) %></h3>
<% end %>

  
